# SQuAD 2.0 dataset translated using google translate api
    Currently only about 1/4th of the train set and almost all of the test set is translated
    Answer start positions are aligned using fuzzy regex matching. Instances where the matching could not be done were removed from the dataset. Therefore the translated dataset is probably mostly correct :).